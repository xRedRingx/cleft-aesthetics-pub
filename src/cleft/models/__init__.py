"""Models.

Empty in Phase 0. When ``factory.py`` arrives it is the only place permitted to
name normalization constants — see ``tests/test_normalization.py``, which guards
a real Stage-1 bug where hardcoded ImageNet mean/std was applied to backbones
that were not trained with it.
"""
