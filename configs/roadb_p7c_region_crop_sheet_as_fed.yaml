# ROAD B BRANCH 3 — THE CROP SHEET (as_fed). THE GATE.
#
# GENERATED by scripts/generate_roadb_configs.py.
#
# **NO BRANCH 3 ARM RUNS UNTIL BOTH SHEETS ARE REVIEWED**
# (brief §5 criterion 3). Whether a box labelled
# `philtral_column` contains a philtral column is checked by eye
# or not at all.
#
# **This sheet shows the crops AS THE BACKBONE RECEIVES THEM.**
#
# **The second sheet, and the one that closes a real gap.**
# `crop_region` resizes every window to 224x224, so a 149x124 box
# arrives at the backbone STRETCHED -- measured ratios span 0.72
# to 1.54 before squaring. The native sheet deliberately hides
# that, so the first review confirmed anatomy and magnification
# while never seeing what the model receives. Captions name the
# source size and the ratio.
#
# It confounds no contrast -- every arm is squared identically,
# and the whole-frame control is squared too -- but a stretched
# philtral column looks fine in the numbers and wrong to a
# clinician (roadb.SHEETS_MUST_SHOW_WHAT_THE_CONSUMER_RECEIVES).
#
# **The random control is rendered too**: it decides what a positive
# finding is ABOUT, so its placement gets the same eye.
#
# CLUSTER-ONLY PNGs (patient faces); patient ids stay in the
# CLUSTER-ONLY size record, never in metrics.json.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_region_crop_sheet_as_fed.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
task:
  kind: roadb_region_crop_sheet
  staged_artifact: roadb_768_nonsquare_g2_v1
  n_patients: 8
  columns: 6
  mode: as_fed
