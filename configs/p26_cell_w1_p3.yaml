# PHASE 26 -- the calibration ablation, cell w1p3.
#
# weight_decay 0.001, patience 30. Every other
# field is the probe's own, carried from
# configs/p7_d1_vit_b16_imagenet_g1.yaml over the UNION of its task block
# rather than over a list, so a field cannot be omitted
# silently (phase25.PHASE_25_CLOSING criterion 2).
#
# max_epochs is held at 30 on every cell and does
# not bind: at the far end patience equals the budget, so
# early stopping cannot fire before it
# (phase26.THE_SECOND_FACTOR_RULED).
#
# GENERATED by scripts/generate_phase26_configs.py. Do not
# hand-edit: --check fails on drift.

schema_version: 1
phase: p26
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_g1_ladder_v1/vit_b16__imagenet__g1
  rollup_sha256: ba4b12535f60aca4cc679246a60ecbcdc880c5ca66319937126e4f85c632d7ab
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  backbone: vit_b16
  init: imagenet
  trainable: head
  learning_rate: 0.001
  batch_size: 32
  max_epochs: 30
  patience: 30
  weight_decay: 0.001
