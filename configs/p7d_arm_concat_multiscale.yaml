# PHASE 7D — ARM 4: CONCATENATED 8+16+32, one factor from the
# singles, no new extraction. Linear probe over the 2304-dim
# concatenation, five seeds, standard ladder criterion.
#
# GENERATED by scripts/generate_phase7d_configs.py. Hyperparameters
# COPIED from p7_d1_vit_b16_imagenet_g1.yaml; vit_b16's set is the control's own
# verified artifact, carried with its hash.
#
# PRE-REGISTERED PRIOR [MEASURED, 2026-08-14, verbatim]: Phase 7B
# measured embedding concatenation at +0.0221 inner-validation and
# -0.0542 out-of-fold. A measured prior AGAINST this arm on this
# cohort, stated where the result will be read
# (phase7b.SEARCH_AXIS_VERDICTS; the loader repeats it in the run
# record).
#
# The embeddings rollups are the declare pass's own figures --
# filled 2026-08-15 against the sets the b8/b32 extractions built, and
# CARRIED by this generator, never typed. If a set is ever
# rebuilt, re-run declare_inputs.py; guard 3 refuses a stale
# hash.
#
# ATTRIBUTION (maintainer, 2026-08-15): arm 6 is the direct response
# to the supervision shared paper (Fan et al., Multiscale Vision
# Transformers, ICCV 2021, shared 2026-08-13); arms 1-5 are the
# project's decomposition of the 'multi-scale, not 16x16' request
# into patch granularity at fixed input, registered before the
# paper was shared. ladder.PHASE_7D_BUILT.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p7d_arm_concat_multiscale.yaml"

schema_version: 1
phase: p7d
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: emb_vit_b8
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_7d_vit_b8_v1/vit_b8__imagenet__g1
  rollup_sha256: 978d0edba316abdc2440f60b881fcd0c9634122abec45db093cd7547c5d6dca8
- name: emb_vit_b16
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_g1_ladder_v1/vit_b16__imagenet__g1
  rollup_sha256: ba4b12535f60aca4cc679246a60ecbcdc880c5ca66319937126e4f85c632d7ab
- name: emb_vit_b32
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_7d_vit_b32_v1/vit_b32__imagenet__g1
  rollup_sha256: 6f0ede762200747ad5c7a09028827fce6f05dc1c200d45e0139c7a864fb8aed7
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  backbone: concat
  init: imagenet
  concat_embeddings_artifacts:
  - artifact: emb_vit_b8
    backbone: vit_b8
  - artifact: emb_vit_b16
    backbone: vit_b16
  - artifact: emb_vit_b32
    backbone: vit_b32
  geometry: g1
  label: mean
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  inner_val_frac: 0.2
  max_epochs: 40
  patience: 5
  monitor: inner_val_mse
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  trainable: head
