# PHASE 7D — ARM: mvitv2_b, ImageNet, G1, whole image, frozen
# backbone + linear probe, five seeds, standard ladder criterion
# (PCC primary).
#
# GENERATED by scripts/generate_phase7d_configs.py. Hyperparameters
# COPIED from p7_d1_vit_b16_imagenet_g1.yaml -- the control at 0.2520 -- so the
# axis varies the backbone and nothing else.
#
# COMMITTED FRAMING (2026-08-15, before any result, verbatim):
# Swin-B already gives this cohort a measured data point on hierarchical multi-scale transformers; MViT tests whether that result is Swin-specific or a property of the hierarchical class.
# Near Swin's cleft result -> class property;
# materially different -> the hierarchy alone does not determine the outcome and the mechanisms differ (pooling attention vs shifted windows).
# Neither reading is 'MViT should win': the paper's ImageNet margins are from full training on 1.28M images; the frozen-probe regime at n=237 is a different question.
# POOLING: mean over the final stage's 49 tokens -- NO cls token --
# and 768-dim, both MEASURED off the built model, so the head width
# is deliberate rather than defaulted.
#
# The embeddings rollup is the declare pass's own figures --
# filled 2026-08-15 against the sets p7d_extract_mvitv2_b.yaml built, and
# CARRIED by this generator, never typed. If a set is ever
# rebuilt, re-run declare_inputs.py; guard 3 refuses a stale
# hash.
#
# ATTRIBUTION (maintainer, 2026-08-15): arm 6 is the direct response
# to the supervision shared paper (Fan et al., Multiscale Vision
# Transformers, ICCV 2021, shared 2026-08-13); arms 1-5 are the
# project's decomposition of the 'multi-scale, not 16x16' request
# into patch granularity at fixed input, registered before the
# paper was shared. ladder.PHASE_7D_BUILT.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p7d_arm_mvitv2_b.yaml"

schema_version: 1
phase: p7d
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_7d_mvitv2_b_v1/mvitv2_b__imagenet__g1
  rollup_sha256: cd8385e220fb8db195d9aa4452cd95e88c07bc8cb5af3bcab4a2fa6d0c7866ce
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  backbone: mvitv2_b
  init: imagenet
  geometry: g1
  label: mean
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  inner_val_frac: 0.2
  max_epochs: 40
  patience: 5
  monitor: inner_val_mse
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  trainable: head
