# ROAD B BRANCH 3 ARM — anatomy_graph_srgnn
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.regioncrop_arms().
#
# does message passing over anatomy add anything
#
# Reads roadb_emb_crops_srgnn_anatomy_g2_768nsq_v1
# and declares **consume: nodes** (embeddings.as_consumed).
# One region_vectors set serves BOTH concat and nodes, so the shape
# is DECLARED rather than inferred: a graph arm handed a flattened
# vector would train happily and report a number. Flattening is C
# order -- 22 contiguous blocks of D, in the set's own `regions`
# order, which is what makes a flattened vector interpretable.
#
# **Pooled to 2048 per crop, and NOT SR-GNN as published**
# (roadb.GRAPH_ARM_POOLS_TO_VECTORS): pooling makes
# SeqSelfAttention a smaller layer. This arm tests message passing
# over anatomy-crop embeddings, not the published architecture.
#
# Frozen backbone, head-only, 10 seeds (graph regime).
# Reports its OWN seed SD (4.12.1). The bar is unchanged: this cohort
# resolves ViT-collapse-sized effects and nothing smaller.
#
# embeddings RESOLVED from the declare pass.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_arm_anatomy_graph_srgnn.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_crops_srgnn_anatomy_g2_768nsq_v1/srgnn__imagenet__anatomy
  rollup_sha256: 27243879a88bc355fdbb3dbb78218ad31d52beb3be451c9099359db58630ddd2
task:
  kind: train_graph_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_768_nonsquare_g2_v1
  embeddings_artifact: embeddings
  geometry: g2
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  - 42
  - 271828
  - 314159
  - 161803
  - 777
  backbone: srgnn
  init: imagenet
  region_scheme: native
  trainable: graph_layers
  learning_rate: 0.0001
  weight_decay: 0.01
  batch_size: 16
  consume: nodes
