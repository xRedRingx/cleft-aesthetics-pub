# ROAD B PHASE 7 ARM — vit_b16 masked at 224
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family vit_b16__scut_masked | transformer regime, 5 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_vit_b16_masked_g1_224_v1
# reporting: descriptive with the draw caveat inline
#
# The task block is p7_c_swin_b_scut_masked_g1.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# Checkpoint entry copied VERBATIM from the extraction config -- the
# run refuses a mismatch between the set's recorded checkpoint and
# the declared one, and copying is how the two configs cannot
# disagree.
#
# Not gated by the sweep: frozen-head fits are the regime
# SEED_BAND_RESOLVED already banded across resolutions -- zero new
# bands.
#
# **DESCRIPTIVE, decided before any arm ran**: this cell is ONE
# checkpoint draw (roadb.VIT_DRAW_PICKS names it; the measured draw
# spread -- 0.055 at 224, 0.027 at 512 -- rides ON TOP of this arm's
# seed SD, and the arm cannot see it). No trend claim from this
# family.
#
# **FAMILY REPORT GATE** (roadb.PHASE_7_PARTIAL_LAUNCH): this arm
# RUNS now, but its family reports only when the 768 point lands --
# a two-point family is a line, not a shape.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_vit_b16_masked_224.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_vit_b16_masked_g1_224_v1/vit_b16__scut_masked__g1
  rollup_sha256: 8a9e51ebf7d3f9b6fdfb912061c13b3d6600d598cd1f78ae4a4af0c47729678e
- name: ckpt_vit_b16_masked_g1
  path: /home/user/codex/cleft-aesthetics/runs/keeper/roadb_p6/roadb_p6_pretrain_vit_b16_masked_g1_224__5b784842__roadb-p6-vit-224-unfused/pretrained.npz
  rollup_sha256: b89bebcca41a5cd3f0c21be5c97458adbe1b5d7194bf0488e8d25b23870f39d5
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  checkpoint: ckpt_vit_b16_masked_g1
  backbone: vit_b16
  init: scut_masked
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
