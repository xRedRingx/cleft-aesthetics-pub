# ROAD B PHASE 7 ARM — agnet imagenet at 512
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family agnet__imagenet | graph regime, 10 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_agnet_imagenet_g1_512_v1
# reporting: family shape claim, per-step deltas co-reported
#
# The task block is p7_d1_agnet_imagenet_g1_native.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# **LAUNCH GATE: waits on the graph band sweep's READING**
# (roadb.PHASE_7_GRAPH_BAND_SWEEP). Inside the F-interval -> proceed;
# outside -> per-resolution graph bands and the design re-reads
# first.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_agnet_imagenet_512.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_agnet_imagenet_g1_512_v1/agnet__imagenet__g1
  rollup_sha256: aace6e2f8bd3a27ffcd949077c6f917c1ed3eff4f00af574270b55318a45289c
task:
  kind: train_graph_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  - 42
  - 271828
  - 314159
  - 161803
  - 777
  backbone: agnet
  init: imagenet
  region_scheme: native
  trainable: graph_layers
  deterministic: false
  learning_rate: 0.0001
  weight_decay: 0.01
  batch_size: 16
