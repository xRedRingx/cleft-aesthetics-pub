# ROAD B PHASE 7 ARM — swin_b masked at 512
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family swin_b__scut_masked | transformer regime, 5 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_swin_b_masked_g1_512_v1
# reporting: family shape claim, per-step deltas co-reported
#
# The task block is p7_c_swin_b_scut_masked_g1.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# Checkpoint entry copied VERBATIM from the extraction config -- the
# run refuses a mismatch between the set's recorded checkpoint and
# the declared one, and copying is how the two configs cannot
# disagree.
#
# Not gated by the sweep: frozen-head fits are the regime
# SEED_BAND_RESOLVED already banded across resolutions -- zero new
# bands.
#
# **FAMILY REPORT GATE** (roadb.PHASE_7_PARTIAL_LAUNCH): this arm
# RUNS now, but its family reports only when the 768 point lands --
# a two-point family is a line, not a shape.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_swin_b_masked_512.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_swin_b_masked_g1_512_v1/swin_b__scut_masked__g1
  rollup_sha256: e43532b049b06c482591e4f0bf6a4a056358f0d1bf46c05462b801e99acc0822
- name: ckpt_swin_b_masked_g1
  path: /home/user/codex/cleft-aesthetics/runs/keeper/roadb_p6/roadb_p6_pretrain_swin_b_masked_g1_512__0cd45b26__roadb-p6-swin-512-2/pretrained.npz
  rollup_sha256: e63d626eb8027f5adc7aef8a8d4f0f46ca6400d37afcfe6683fa2f3f4716bcb8
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  checkpoint: ckpt_swin_b_masked_g1
  backbone: swin_b
  init: scut_masked
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
