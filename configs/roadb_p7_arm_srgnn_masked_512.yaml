# ROAD B PHASE 7 ARM — srgnn masked at 512
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family srgnn__scut_masked | graph regime, 10 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_srgnn_masked_g1_512_v1
# reporting: family shape claim, per-step deltas co-reported
#
# The task block is p7_d1_srgnn_scut_masked_g1_native.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# Checkpoint entry copied VERBATIM from the extraction config -- the
# run refuses a mismatch between the set's recorded checkpoint and
# the declared one, and copying is how the two configs cannot
# disagree.
#
# **THE SWEEP DOUBLES AS THIS ARM'S RUN** (roadb_p7_graph_seed_band:
# same cell, same ten seeds, task block identical by construction).
# Relaunching this config after the sweep duplicates it bit-for-bit;
# read this arm's numbers from the sweep's run directory.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_srgnn_masked_512.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_srgnn_masked_g1_512_v1/srgnn__scut_masked__g1__native
  rollup_sha256: 98b81ef6eab1f3ca520e3a33fa7b871f2930415541d6ee1f882e351663cbcbdf
- name: ckpt_srgnn_native_masked_g1
  path: /home/user/codex/cleft-aesthetics/runs/keeper/roadb_p6/roadb_p6_pretrain_srgnn_native_masked_g1_512__fe701703__roadb-p6-srgnn-512/pretrained.npz
  rollup_sha256: a8f26b2d5bfe53ca82df2afcf1c92a2a9871b0e5b1c4ff218a002b6b57c69161
task:
  kind: train_graph_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  - 42
  - 271828
  - 314159
  - 161803
  - 777
  checkpoint: ckpt_srgnn_native_masked_g1
  backbone: srgnn
  init: scut_masked
  region_scheme: native
  trainable: graph_layers
  learning_rate: 0.0001
  weight_decay: 0.01
  batch_size: 16
