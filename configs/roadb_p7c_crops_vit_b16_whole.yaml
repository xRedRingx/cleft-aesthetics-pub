# ROAD B BRANCH 3 — THE CONTROL (whole frame, 768 NON-SQUARE G2)
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.regioncrop_arms().
#
# **THE BRIEF'S CONTROL WAS MISMATCHED AND THIS REPLACES IT.**
# §3 nominated Road B's existing 768 ViT arm, which reads
# roadb_768_square_g1_v1 -- while the crops come from NON-SQUARE G2
# (§2.1: at G1 the lateral anatomy regions fall 76.4% on white).
# Comparing them would vary cropping, aspect AND geometry at once --
# the matched-pipeline trap roadb.PHASE_7_RESOLUTION_PAIRING records.
#
# **[CORRECTED 2026-08-14] It runs through the CROP path, not the
# extraction task.** The first attempt used extract_embeddings and
# died in phase3.load_inputs: that path wants staged_patient_g2.npy,
# the STACKED tensor, and a non-square artifact can never have one --
# the shapes differ per patient, and stacking them was one of the
# five original stage-task defects. Even given a loader, non-square
# frames cannot batch and would give every patient a different ViT
# token count.
#
# So the control is ONE region that is the whole content box, cut and
# resized to 224 exactly as each crop is. crop-vs-whole then differs
# in CONTENT ALONE -- one whole face against 22 magnified regions --
# rather than in content plus batching plus token count, and the
# control inherits the same square stretch the crops get, which is
# what makes it a comparator rather than a confound.
#
# Output is an ordinary POOLED set (n_patients, dim), the shape
# train_cv reads -- its single region is not a region.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_crops_vit_b16_whole.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
- name: pretrained_init
  path: /home/user/codex/cleft-aesthetics/data/inits/init_vit_b16_v1
  rollup_sha256: c974c782e08b8791a3ecec38c5c3b96a919681a3576fc0808abb395fc94c1ea7
task:
  kind: roadb_region_crop_extract
  staged_artifact: roadb_768_nonsquare_g2_v1
  out_version: roadb_emb_crops_vit_b16_whole_g2_768nsq_v1
  backbone: vit_b16
  init: imagenet
  layout: whole
  batch_size: 32
