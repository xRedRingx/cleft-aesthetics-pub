# PHASE 17 -- FULL-SCUT SYNTHESIS SET AT G1, PIECEWISE_AFFINE.
# phase17.EXIT_CRITERIA criterion 1.
#
# GENERATED by scripts/generate_phase17_configs.py.
#
# Warp family: arm B's piecewise affine (scut.piecewise); SAME control points, directions
# and magnitudes either way, so B-vs-C isolates the
# transformation family. G1 only -- the probe's geometry,
# like-for-like with the 0.2520 bar; G2 is UNREGISTERED for
# this phase. n_faces 0 = every SCUT face; sides balanced
# deterministically from the seed.
#
# Set size ruled UNCONSTRAINED by the compute-gate
# measurement (COMPUTE_GATE_MEASUREMENT['outcome_2026_08_29']:
# 0.2-1.2 s/face, no pathology). Per-face timing lines are
# in the task and stay.
#
# CPU work; GPUs idle -- expected, not a defect.
#
# **RESOLVED.** Every input declared at a real path with a
# verified hash.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p17_synth_pwa_g1.yaml"

schema_version: 1
phase: p17
tier: keeper
seed: 1337
inputs:
- name: scut_root
  path: ${CLEFT_SCUT_ROOT}
  rollup_sha256: 1041ceed0ef458e97c8be845eda30cf7957067fa730a4975a0f83bd8ed4947ec
task:
  kind: asymmetry_synthesis
  scut_root: scut_root
  geometries:
  - g1
  n_faces: 0
  n_sheet: 6
  magnitudes:
  - 0.0
  - 0.015
  - 0.025
  - 0.035
  warp_family: piecewise_affine
  write_artifact: true
  out_version: synth_pwa_g1_v1
