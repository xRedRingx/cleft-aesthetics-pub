# ROAD B BRANCH 3 — CROP EXTRACTION (srgnn, anatomy)
#
# GENERATED by scripts/generate_roadb_configs.py.
#
# **RUNS ONLY AFTER THE SHEET IS REVIEWED** (roadb_p7c_region_crop_sheet):
# whether a box labelled philtral_column contains one is an eye
# question, and no arm may consume crops that failed it.
#
# 22 anatomy regions per patient, cut from the non-square G2
# staging and resized to the 224 backbone input -- the
# magnification is in the CROP, not the input size. Extracted
# **ONCE**: frozen backbone, head-only fits downstream, so every
# arm reads this set and nothing re-extracts per epoch.
#
# **No new torch surface**: the crops are flattened into one
# stack and handed to the same extract_features every Road B set
# used, then reshaped to (n_patients, 22, dim) and saved as
# kind `region_vectors` with its ORDERED region names, so a
# consumer asks which column is philtral_column rather than
# trusting position (embeddings.REGION_VECTORS_IS_NOT_PER_REGION).
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_crops_srgnn_anatomy.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
- name: pretrained_init
  path: /home/user/codex/cleft-aesthetics/data/inits/init_srgnn_v1
  rollup_sha256: 2c48719cb27b061b43c5ff9a7b75415399ecd92a8b1e111c46b1a6cb20e89cea
task:
  kind: roadb_region_crop_extract
  staged_artifact: roadb_768_nonsquare_g2_v1
  out_version: roadb_emb_crops_srgnn_anatomy_g2_768nsq_v1
  backbone: srgnn
  init: imagenet
  layout: anatomy
  batch_size: 32
