# ROAD B PHASE 7 ARM — swin_b imagenet at 224
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family swin_b__imagenet | transformer regime, 5 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_swin_b_imagenet_g1_224_v1
# reporting: family shape claim, per-step deltas co-reported
#
# The task block is p7_d1_vit_b16_imagenet_g1.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# Not gated by the sweep: frozen-head fits are the regime
# SEED_BAND_RESOLVED already banded across resolutions -- zero new
# bands.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_swin_b_imagenet_224.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_swin_b_imagenet_g1_224_v1/swin_b__imagenet__g1
  rollup_sha256: 2352a7fc318da75f3003e6b55c3740c0f0f9b126fc2ec43a92010c0d916476dc
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  backbone: swin_b
  init: imagenet
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
