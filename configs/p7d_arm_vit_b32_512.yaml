# PHASE 7D — ARM 5: vit_b32 at 512, SQUARE G1, whole image, frozen
# backbone + linear probe, five seeds, standard ladder criterion
# (PCC primary). The mechanism-discriminating arm.
#
# GENERATED by scripts/generate_phase7d_configs.py. Hyperparameters
# COPIED from p7_d1_vit_b16_imagenet_g1.yaml; staging [DECIDED 2026-08-15,
# maintainer]: SQUARE, roadb_512_square_g1_v1 -- the comparator's own
# artifact, hash carried from its shipped config, so the
# patch16@512 comparison varies patch size and NOTHING else.
#
# COMMITTED READING (2026-08-14, verbatim from
# ladder.PHASE_7D_PATCH_AXIS_REGISTERED). patch32@512 and
# patch16@512 share the IDENTICAL 2.286x position-grid stretch
# (7x7 -> 16x16 against 14x14 -> 32x32). This arm CANNOT 'beat the
# cliff by milder interpolation' -- that rationale was arithmetic
# error, caught and recorded before anything ran
# (amendment_arithmetic_corrected). What it does is DISCRIMINATE:
# cliffs like patch16@512 -> the cliff tracks RELATIVE grid stretch, shared at 2.286; the interpolation mechanism is supported in its ratio form;
# does not cliff -> relative stretch is not the mechanism -- absolute token geometry or patch footprint is. The interpolation account survives only as a stated revision.
# Either way: the arm discriminates mechanisms. Neither outcome licenses tuning, and the arm may not be re-read as 'the configuration that beat the cliff' -- that framing died with the 14x14 claim.
#
# The embeddings rollup is the declare pass's own figures --
# filled 2026-08-15 against the sets p7d_extract_vit_b32_512.yaml built, and
# CARRIED by this generator, never typed. If a set is ever
# rebuilt, re-run declare_inputs.py; guard 3 refuses a stale
# hash.
#
# ATTRIBUTION (maintainer, 2026-08-15): arm 6 is the direct response
# to the supervision shared paper (Fan et al., Multiscale Vision
# Transformers, ICCV 2021, shared 2026-08-13); arms 1-5 are the
# project's decomposition of the 'multi-scale, not 16x16' request
# into patch granularity at fixed input, registered before the
# paper was shared. ladder.PHASE_7D_BUILT.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p7d_arm_vit_b32_512.yaml"

schema_version: 1
phase: p7d
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_7d_vit_b32_512_v1/vit_b32__imagenet__g1
  rollup_sha256: 71d2c8bd26a8503d999a749dac0a25d19d29bd1ecb0c8302c89f6e18c2fa7986
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  embeddings_artifact: embeddings
  backbone: vit_b32
  init: imagenet
  geometry: g1
  label: mean
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  inner_val_frac: 0.2
  max_epochs: 40
  patience: 5
  monitor: inner_val_mse
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  trainable: head
