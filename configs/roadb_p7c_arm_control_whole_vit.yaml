# ROAD B BRANCH 3 — THE CONTROL ARM (whole frame, 768 non-square G2)
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.regioncrop_arms().
#
# **The real control**, replacing the brief's mismatched one -- which
# read square G1 against crops from non-square G2, varying cropping,
# aspect and geometry at once. Task block is
# p7_d1_vit_b16_imagenet_g1.yaml's, with only the staging, geometry
# and seed count changed -- asserted field-for-field by test.
#
# Frozen, head-only, five seeds (transformer regime,
# roadb.PHASE_7_SEED_COUNTS). Reports its OWN seed SD; bands plan
# and never denominate (4.12.1).
#
# **The bar this branch must clear is recorded before it runs**
# (roadb.ROAD_B_PHASE_7_CLOSING): this cohort resolves effects of
# ViT-collapse size and nothing smaller. A 0.03 improvement will
# not be claimable however real it is.
#
# embeddings RESOLVED from the declare pass.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_arm_control_whole_vit.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_crops_vit_b16_whole_g2_768nsq_v1/vit_b16__imagenet__whole
  rollup_sha256: cdc7b93045accc78a3efd3b9c13da4e04b523d571027d7a436a1c6be81352372
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_768_nonsquare_g2_v1
  embeddings_artifact: embeddings
  geometry: g2
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  backbone: vit_b16
  init: imagenet
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  consume: pooled
