# ROAD B PHASE 7 — THE GRAPH-HEAD SEED BAND SWEEP. RUNS BEFORE ANY
# GRAPH ARM.
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.PHASE_7_GRAPH_BAND_SWEEP.
#
# TEN seeds of the srgnn masked-512 arm. The task block below is
# roadb_p7_arm_srgnn_masked_512.yaml's, identical by construction (one
# generator function builds both), so this run DOUBLES AS that arm's
# run: relaunching the arm config afterwards would duplicate it
# bit-for-bit. train_graph_cv reports seed_variance.json from its own
# ten seeds -- the p6_srgnn_seed_band procedure.
#
# **F-COMPARED against Road A's measured graph band**: SD 0.025053,
# ten seeds, run p6-srgnn-seedband-1
# (graph_cleft.MEASURED_GRAPH_SEED_BAND). Same ladder.SEED_POOL ten, so
# the comparison is like-for-like. The statistic is the F-based 95% CI
# on SD_roadB/SD_roadA -- F(9,9), the observed ratio x [1/2.01, 2.01].
#
# **BOTH READINGS, REGISTERED BEFORE THE RUN** (the Phase 3 discipline
# -- SEED_BAND_DESIGN.decision_rule_preregistered: a window chosen
# after seeing the result is a forking path):
#
#   * CI CONTAINS 1 -> the sweep cannot distinguish the regimes: ONE
#     graph planning band, the LARGER of the two SDs (the
#     conservatism Phase 3's three-point application settled), and
#     the graph arms PROCEED.
#   * CI EXCLUDES 1 -> per-resolution graph bands, and the design
#     RE-READS before any further graph arm launches. Transformer
#     arms are not gated either way (SEED_BAND_RESOLVED covers them,
#     zero new bands).
#
# Sensitivity, stated with the rule: ten-vs-ten F(9,9) detects only
# ~2x divergence.
#
# **What the seed varies** -- the SAME three things as Road A's band,
# NOT graph-layer init (warm-started from the paired checkpoint,
# byte-identical across seeds): classifier init, batch order, and the
# inner-val split. Quote all three when quoting either band (4.12.1).
#
# **ALL FOUR INPUT HASHES ARE REAL — READY TO LAUNCH.** The
# embeddings rollup came from the declare pass over the built set;
# the checkpoint entry is the extraction config's, resolved.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_graph_seed_band.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_srgnn_masked_g1_512_v1/srgnn__scut_masked__g1__native
  rollup_sha256: 98b81ef6eab1f3ca520e3a33fa7b871f2930415541d6ee1f882e351663cbcbdf
- name: ckpt_srgnn_native_masked_g1
  path: /home/user/codex/cleft-aesthetics/runs/keeper/roadb_p6/roadb_p6_pretrain_srgnn_native_masked_g1_512__fe701703__roadb-p6-srgnn-512/pretrained.npz
  rollup_sha256: a8f26b2d5bfe53ca82df2afcf1c92a2a9871b0e5b1c4ff218a002b6b57c69161
task:
  kind: train_graph_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  - 42
  - 271828
  - 314159
  - 161803
  - 777
  checkpoint: ckpt_srgnn_native_masked_g1
  backbone: srgnn
  init: scut_masked
  region_scheme: native
  trainable: graph_layers
  learning_rate: 0.0001
  weight_decay: 0.01
  batch_size: 16
