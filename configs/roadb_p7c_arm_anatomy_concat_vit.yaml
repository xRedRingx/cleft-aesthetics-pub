# ROAD B BRANCH 3 ARM — anatomy_concat_vit
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.regioncrop_arms().
#
# does cropping to anatomy beat a whole frame
#
# Reads roadb_emb_crops_vit_b16_anatomy_g2_768nsq_v1
# and declares **consume: concat** (embeddings.as_consumed).
# One region_vectors set serves BOTH concat and nodes, so the shape
# is DECLARED rather than inferred: a graph arm handed a flattened
# vector would train happily and report a number. Flattening is C
# order -- 22 contiguous blocks of D, in the set's own `regions`
# order, which is what makes a flattened vector interpretable.
#
# Frozen backbone, head-only, 5 seeds (transformer regime).
# Reports its OWN seed SD (4.12.1). The bar is unchanged: this cohort
# resolves ViT-collapse-sized effects and nothing smaller.
#
# embeddings RESOLVED from the declare pass.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7c_arm_anatomy_concat_vit.yaml"

schema_version: 1
phase: roadb_p7c
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_768_nonsquare_g2_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_768_nonsquare_g2_v1
  rollup_sha256: 68f34338f9ca5ef0fc7be021cde0df82d44ef8f523f14afd960a945514d8eb9e
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_crops_vit_b16_anatomy_g2_768nsq_v1/vit_b16__imagenet__anatomy
  rollup_sha256: ebba8aebf9af67291bc2b0e459d54fb9d5a0141a9b61ebf88e5bf2275d8189d9
task:
  kind: train_cv
  manifest_artifact: manifest_v1
  staged_artifact: roadb_768_nonsquare_g2_v1
  embeddings_artifact: embeddings
  geometry: g2
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  backbone: vit_b16
  init: imagenet
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  consume: concat
