# PHASE 15 — STOP 4a: CLEFT EMBEDDINGS THROUGH THE
# MASKED_G1 CHECKPOINT. phase15.STOP_4_BUILT.
#
# GENERATED by scripts/generate_phase15_configs.py.
#
# **THE ARM-TO-RUN MAPPING IS ASSERTED, NOT TRUSTED**: the
# task reads the run's OWN metrics.json and refuses unless
# its recorded source is 'masked_g1'. The mapping
# cannot be confirmed from this machine (no cluster
# access), so the check lives where the checkpoint is
# consumed -- a swap fails here, not in a verdict.
#
# **MEBEAUTY'S OWN NAMESPACE.** The set is written by
# mebeauty.save_set with init in MEBEAUTY_INITS; the
# ladder's embeddings.INITS and its 24/12 arithmetic stay
# untouched, and the two lattices are never pooled. Row
# order is still checked with the LADDER's own
# assert_row_order -- the property, reused.
#
# **CLUSTER-ONLY**: cleft-patient embeddings.
#
# **RESOLVED.** Every input declared at a real path with a
# verified hash.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p15_extract_mebeauty_g1.yaml"

schema_version: 1
phase: p15
tier: keeper
seed: 1337
inputs:
- name: pretrain_run
  path: /home/user/codex/cleft-aesthetics/runs/keeper/p15/p15_pretrain_mebeauty_g1__dad0e892__p15-pretrain-mebeauty-g1
  rollup_sha256: 5c606603950fabda04e3af8048f899f0f8fcca0bf2d8510c733293074ff59f22
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
task:
  kind: extract_mebeauty_embeddings
  pretrain_run: pretrain_run
  expect_source: masked_g1
  init: mebeauty_masked
  geometry: g1
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  backbone: vit_b16
  out_version: mebeauty_g1_cleft_v1
  expect_patients: 237
  batch_size: 32
