# PHASE 12 — ARM C — FRONTAL (+) BASAL, 1536-d, 1537-parameter head. The
# phase's question lives here, read against A AND D.
# phase12.STOP_3_REGISTERED; readings phase12.PHASE_12_READINGS;
# both interpretation sentences pre-written
# (phase12.INTERPRETATION_SENTENCES_PREWRITTEN).
#
# GENERATED by scripts/generate_phase12_configs.py. ONE task
# serves all four arms; the `views` channel list is the ONLY
# difference between their configs, so every contrast is
# one-factor by construction. The recipe block is COPIED from
# p7_d1_vit_b16_imagenet_g1.yaml -- the 0.2520 arm's own -- so recipe identity
# holds by construction. Folds: cleft_v1_views, carried verbatim
# from cleft_v1. Cohort: 236, patient 238 excluded from EVERY
# arm including this one.
#
# **RESOLVED.** Every input declared with a verified hash.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p12_arm_c_concat.yaml"

schema_version: 1
phase: p12
tier: keeper
seed: 1337
inputs:
- name: manifest_views
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1_views
  rollup_sha256: 1b0f96bddcbaabf0cd79a08fc18ae106dfecc674ad33a3d6359c122f05338d19
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_g1_ladder_v1/vit_b16__imagenet__g1
  rollup_sha256: ba4b12535f60aca4cc679246a60ecbcdc880c5ca66319937126e4f85c632d7ab
- name: basal_embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_basal_v1/vit_b16__imagenet__g1
  rollup_sha256: 08d376e370a5f62b05d52d30c62245cac422bbe1de21925a891538105dd6914d
task:
  kind: view_arm
  arm: c_two_view_concat
  views:
  - frontal
  - basal
  manifest_artifact: manifest_views
  frontal_embeddings: embeddings
  basal_embeddings: basal_embeddings
  label: mean
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  inner_val_frac: 0.2
  max_epochs: 40
  patience: 5
  monitor: inner_val_mse
  learning_rate: 0.001
  weight_decay: 0.01
  expect_patients: 236
  expect_head_parameters: 1537
