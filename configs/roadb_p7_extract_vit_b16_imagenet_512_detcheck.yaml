# ROAD B PHASE 7 EXTRACTION — vit_b16 imagenet at 512 — THE DETERMINISM CHECK COPY
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_extraction_sets().
#
# ONE set, its OWN artifact (roadb.PHASE_7_EXTRACTION_PLAN grouping):
# Phase 7 consumes selectively, and a shared artifact would stale
# every declaration on any partial re-extraction. The resolution
# lives in the artifact VERSION; the set name repeats across
# resolutions by design.
#
# imagenet extracts from the DECLARED snapshot (init-vintage rule:
# every point of the trend starts from the same verified bytes).
#
# **RUNS SECOND, after its twin.** Same set, out_version *_detcheck_v1
# -- no config ever declares it. Payload rollups byte-equal clears the
# whole batch (on_pass, one recorded decision) and this artifact is
# then DELETED; a difference stops the batch (on_fail).
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_extract_vit_b16_imagenet_512_detcheck.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: roadb_512_square_g1_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/roadb_512_square_g1_v1
  rollup_sha256: fa0ba0269491f33c055463081b81579f7991670860873494c518aa0c87865fca
- name: pretrained_init
  path: /home/user/codex/cleft-aesthetics/data/inits/init_vit_b16_v1
  rollup_sha256: c974c782e08b8791a3ecec38c5c3b96a919681a3576fc0808abb395fc94c1ea7
task:
  kind: extract_embeddings
  manifest_artifact: manifest_v1
  staged_artifact: roadb_512_square_g1_v1
  out_version: roadb_emb_vit_b16_imagenet_g1_512_detcheck_v1
  batch_size: 32
  per_fold_bn_reestimation: false
  sets:
  - backbone: vit_b16
    init: imagenet
    geometry: g1
