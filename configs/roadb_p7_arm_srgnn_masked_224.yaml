# ROAD B PHASE 7 ARM — srgnn masked at 224
#
# GENERATED by scripts/generate_roadb_configs.py from
# roadb.phase7_arms().
#
# family srgnn__scut_masked | graph regime, 10 seeds (roadb.PHASE_7_SEED_COUNTS)
# artifact: roadb_emb_srgnn_masked_g1_224_v1
# reporting: family shape claim, per-step deltas co-reported
#
# The task block is p7_d1_srgnn_scut_masked_g1_native.yaml's in every harness and
# hyperparameter field -- only identity fields differ (backbone, init,
# artifacts, seeds by regime), asserted field-for-field by test. This
# arm reports its OWN seed SD (4.12.1); bands plan, never denominate.
#
# embeddings RESOLVED: the declare pass over the built set
# (roadb.PHASE_7_PARTIAL_LAUNCH.declaration_pass_landed).
#
# Checkpoint entry copied VERBATIM from the extraction config -- the
# run refuses a mismatch between the set's recorded checkpoint and
# the declared one, and copying is how the two configs cannot
# disagree.
#
# **LAUNCH GATE: waits on the graph band sweep's READING**
# (roadb.PHASE_7_GRAPH_BAND_SWEEP). Inside the F-interval -> proceed;
# outside -> per-resolution graph bands and the design re-reads
# first.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/roadb_p7_arm_srgnn_masked_224.yaml"

schema_version: 1
phase: roadb_p7
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/roadb_emb_srgnn_masked_g1_224_v1/srgnn__scut_masked__g1__native
  rollup_sha256: 4b31f7b605404a3d58009fd86f691e966932e10dee60a31a7d15ef1260484b45
- name: ckpt_srgnn_native_masked_g1
  path: /home/user/codex/cleft-aesthetics/runs/keeper/roadb_p6/roadb_p6_pretrain_srgnn_native_masked_g1_224__fe701703__roadb-p6-srgnn-224/pretrained.npz
  rollup_sha256: 12d8ae2123939817eacc2193076e28740f7919aa52bb449150ce7bdaaac61f37
task:
  kind: train_graph_cv
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  - 42
  - 271828
  - 314159
  - 161803
  - 777
  checkpoint: ckpt_srgnn_native_masked_g1
  backbone: srgnn
  init: scut_masked
  region_scheme: native
  trainable: graph_layers
  learning_rate: 0.0001
  weight_decay: 0.01
  batch_size: 16
