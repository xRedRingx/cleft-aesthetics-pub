# PHASE 13 ADDENDUM P3: RECONSTRUCTIONS WITH THE NOSE
# THIRD BLANKED, RE-EMBEDDED. phase13.P3_TRIGGERED -- the
# registered condition FIRED (P2: nose -0.082, lips -0.162)
# and the maintainer said run.
#
# GENERATED by scripts/generate_phase13_configs.py. THREE
# configs share ONE kind and differ in `occluded_band` ALONE.
#
# **RECONSTRUCTIONS ARE REGENERATED IN MEMORY** through the
# epoch-20 checkpoint (the shared gate-first helper), occluded
# by the same band_rows thirds P2 used on originals, and
# never persisted -- only the CLUSTER-ONLY embedding set lands.
#
# **THE BAND-SMEARING CAVEAT RIDES AS REGISTERED**: the
# decoder's errors are band-smeared (the 3.23x everywhere-
# gap), so per-band attribution on reconstructions is a
# BLUNTER instrument than on originals.
#
# **cleft_reconstruction_acknowledged: true -- A DATED
# CONFIRMATION, INHERITED.** This task REGENERATES cohort
# reconstructions, squarely inside the gate's own trigger.
#
# The probe on this set is a SEPARATE run (p13_probe_occluded_recon_nose.yaml),
# A's recipe byte-identical. A GPU job.
#
# **RESOLVED.** Every input declared at a real path with a
# verified hash.
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p13_occlude_recon_nose.yaml"

schema_version: 1
phase: p13
tier: keeper
seed: 1337
inputs:
- name: decoder_run
  path: /home/user/codex/cleft-aesthetics/runs/keeper/p13/p13_scut_decoder__7990c89e__p13-scut-decoder-2
  rollup_sha256: f0618ba9acf264b5f40a0672d8998dc157c6bdb692c2c39828a7d625a03e1dff
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_g1_ladder_v1/vit_b16__imagenet__g1
  rollup_sha256: ba4b12535f60aca4cc679246a60ecbcdc880c5ca66319937126e4f85c632d7ab
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
task:
  kind: extract_occluded_recon_embeddings
  occluded_band: nose
  decoder_run: decoder_run
  checkpoint_epoch: 20
  cleft_embeddings: embeddings
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  backbone: vit_b16
  init: imagenet
  geometry: g1
  out_version: embeddings_occluded_recon_nose_v1
  cleft_reconstruction_acknowledged: true
  expect_patients: 237
  batch_size: 32
  extract_batch_size: 32
