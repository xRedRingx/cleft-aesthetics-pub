# PHASE 20 -- ARM S: THE STRATIFIED PERMUTATION. THE ATTRIBUTION.
#
# GENERATED by scripts/generate_phase20_configs.py. Do not
# hand-edit: --check re-derives and diffs, and every input is
# CARRIED VERBATIM from a shipped config, so a hand edit is how
# a control quietly stops being the arm it controls.
#
# The probe's config with the panel-mean labels shuffled WITHIN
# STRATA of the confound model's fitted value, so the confound
# structure survives and the cleft-specific structure does not.
#
# **RUNS ONLY IF ARM P PASSES** (phase20.ORDERING_BOUND).
#
# k_strata: 10 -- LOCKED 2026-08-31
# (phase20.STRATIFICATION_RULED). The variable is the IN-SAMPLE
# fitted value of a ridge over all five confound statistics
# against the panel mean; it is NOT a config field, because a
# pre-registration a config could edit is not one. k is a
# locked scientific setting: never tuned across runs, movement
# requires a dated amendment, and the loader refuses any other
# value (_check_permutation).
#
# Expected fixed points: 10 of 237 (4.2%) -- E[fixed] = 1 per
# stratum for ANY stratum size. Pre-registered, reported per
# seed, never discarded and never redrawn.
#
# NO LEDGER ROW (phase20.LEDGER_RULED): the quantity is an
# ATTRIBUTION, not a method comparison. Reported descriptively
# against the confound ceiling and 0.2520, with the residual
# read against ladder.SMALLEST_RESOLVABLE_DIFFERENCE (0.1386).
#
#   bash -lc "cd /home/user/codex/cleft-aesthetics && PYTHONPATH=src \
#     python -m cleft.run --config configs/p20_permutation_stratified.yaml"

schema_version: 1
phase: p20
tier: keeper
seed: 1337
inputs:
- name: manifest_v1
  path: /home/user/codex/cleft-aesthetics/data/manifests/cleft_v1
  rollup_sha256: fb5177b31f2aa61bfac25f8a528004bf2075adb83128516a002e29eafbee0aff
- name: staged_v1
  path: /home/user/codex/cleft-aesthetics/data/staged/staged_v1
  rollup_sha256: a06401b9b474c241182e07d697939a8903bdf92c9c470fdfe7a423add155a406
- name: embeddings
  path: /home/user/codex/cleft-aesthetics/data/embeddings/embeddings_g1_ladder_v1/vit_b16__imagenet__g1
  rollup_sha256: ba4b12535f60aca4cc679246a60ecbcdc880c5ca66319937126e4f85c632d7ab
task:
  kind: p20_permutation
  manifest_artifact: manifest_v1
  staged_artifact: staged_v1
  embeddings_artifact: embeddings
  geometry: g1
  label: mean
  max_epochs: 40
  patience: 5
  inner_val_frac: 0.2
  monitor: inner_val_mse
  seeds:
  - 1337
  - 2024
  - 7
  - 99
  - 12345
  backbone: vit_b16
  init: imagenet
  trainable: head
  learning_rate: 0.001
  weight_decay: 0.01
  batch_size: 32
  permutation:
    kind: stratified
    k_strata: 10
